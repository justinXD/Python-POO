# SOn iguales a las listas con la diferencia de que no se pueden modificar
numeros = (1, 2, 3) + (4, 5, 6)
print(numeros)

# Tupla de una lista
punto = tuple([1, 2])

menosNumeros = numeros[:2]
print(menosNumeros)

# Lista en base a la tupla
listaNumeros = list(numeros)
